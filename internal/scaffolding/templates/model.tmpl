// Package domain contains the core entities and value objects of Kodia Framework.
// This is a domain entity with no external dependencies — only pure Go stdlib.
package domain

import (
	"time"
)

// {{.Name}} is a domain entity representing a {{.LowerName}}.
type {{.Name}} struct {
	ID        uint64    `json:"id"`
	{{- range .Fields}}
	{{.Name}} {{.GoType}} `json:"{{.LowerName}}"`
	{{- end}}
	CreatedAt time.Time `json:"created_at"`
	UpdatedAt time.Time `json:"updated_at"`
	DeletedAt *time.Time `json:"deleted_at,omitempty"`
}
