package factories

import (
	"github.com/kodia-studio/kodia/internal/core/domain"
	"github.com/kodia-studio/kodia/pkg/database"
)

func init() {
	database.Define(domain.{{.Name}}{}, func() interface{} {
		// Use gofakeit functions directly (e.g. gofakeit.Name(), gofakeit.Email())
		
		return &domain.{{.Name}}{
			// ID: fake.UUID(), // Auto-generated by BaseModel
			// Add your fake fields here
			// Name: fake.Name(),
		}
	})
}
